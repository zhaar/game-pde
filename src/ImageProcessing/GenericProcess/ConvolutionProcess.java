package ImageProcessing.GenericProcess;

import processing.core.PApplet;

public class ConvolutionProcess extends GenericImageProcess {
    public ConvolutionProcess(PApplet context, PixelFunction pixelFunction) {
        super(context, pixelFunction);
    }
}
